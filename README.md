
# Ecommerce Backend

## Setup Instructions

1. Install dependencies
   npm install

2. Create .env file
   PORT=5000
   JWT_SECRET=your_super_secret_key

3. Run the server
   npm start

Server runs at http://localhost:5000
